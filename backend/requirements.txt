# ===================================================
# TANGARA 2026 - Backend (FastAPI + ClickHouse)
# ===================================================
fastapi==0.115.6
uvicorn[standard]==0.34.0
clickhouse-connect==0.8.11
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
cachetools==5.5.0
httpx==0.28.1
geopy==2.4.1

# Router de intent de MuadDib. Pineado a un commit SHA, no a la rama
# "dev" (que se mueve a diario) -- mismo criterio que ya usamos para
# las GitHub Actions. Esto pinea la LIBRERÍA (router.py, embeddings.py,
# etc.) -- la config del cliente (backend/app/muaddib_client/*.yaml) ya
# no es una copia sincronizada de MuadDib, vive y se edita acá
# directamente (2026-09-20, se retiró SYNC.py).
#
# 2026-09-21: bump a 12b0781 -- motor de Nivel 2 (escalada con tool-use
# de Claude). Trae anthropic y jsonschema como dependencias transitivas.
#
# 2026-09-22: bump a 8a62a12 -- resolve_async() (Nivel 1 en un executor
# propio, no bloquea el loop del caller) + Herramienta.funcion puede ser
# sync o async (el motor detecta solo, corre las sync en asyncio.to_thread).
# Medido en la prueba de carga: bajó el bloqueo del event loop de chat.py
# de 184 eventos (peor 1.4s) a 4 (peor 0.55s) solo con el fix de caché de
# EcoPulse -- este bump es para ver si resuelve el resto.
git+https://github.com/N-1000/MuadDib.git@8a62a12b4c97ed158beb7435f5c741d6b6379ba8#egg=muaddib
